export interface ProjectProps {
  id: number;
  name: string;
  description: string;
  html_url: string;
}

export interface Project {
  id: number;
  name: string;
  description: string;
  html_url: string;
}
